 float p[50];
    //  float cost;
    // for (int i=0;i<n;i++)
    // {
    //     p[i]=b[i].price;
    // }
    // cost=expensive(p,n); //function call to find highest number from array P which is the expensive book

